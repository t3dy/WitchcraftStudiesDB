# WitchcraftStudiesDB Dependencies

# PDF Processing
pdfplumber>=0.10.0          # Primary PDF extraction (handles complex layouts)
PyPDF2>=4.0.0               # Fallback PDF reader
pypdf>=3.16.0               # Alternative to PyPDF2

# Text Processing
regex>=2023.10.3            # Advanced regex for document parsing
python-docx>=0.8.11         # For DOCX output (future)
markdown>=3.5.0             # For Markdown validation/processing

# Data Handling
pandas>=2.0.0               # For CSV/Excel data integration
openpyxl>=3.10.0            # Excel support
jsonschema>=4.20.0          # JSON schema validation

# Optional OCR (for scanned PDFs)
pytesseract>=0.3.10         # OCR interface
Pillow>=10.0.0              # Image processing for OCR

# Utilities
tqdm>=4.66.0                # Progress bars
click>=8.1.0                # CLI framework
python-dotenv>=1.0.0        # Environment configuration
requests>=2.31.0            # HTTP for downloading papers

# Development/Testing
pytest>=7.4.0               # Testing framework
black>=23.10.0              # Code formatting
ruff>=0.1.0                 # Linting
mypy>=1.6.0                 # Type checking
